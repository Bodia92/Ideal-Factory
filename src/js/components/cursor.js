// const cursorBuild = () => {
// 	let cursorBody = document.querySelector('.slider_fullscreen');
// 	let cursor = document.querySelector('.slider_fullscreen__cursor');
// 	cursorBody.addEventListener('mousemove', (e) => {
// 		cursor.style.cssText = `left: ${e.clientX}px; top: ${e.clientY}px;`;
// 	});
// };

// export default cursorBuild;
